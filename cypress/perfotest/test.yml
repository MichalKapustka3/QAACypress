config:
  target: "http://httpbin.org/"
  phases:
    - duration: 60
      arrivalRate: 1
      name: Artillery First Test
scenarios:
  - name: Getting a resource
    flow:
      - get:
          url: "{{ target }}get"
